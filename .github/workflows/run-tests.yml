name: build
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: do-the-tests
        run: |
          mkdir build_TMP
          cmake -S . -B build_TMP
          cmake --build build_TMP --target check

