name: build
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      - name: do-the-tests
        run: |
          mkdir build
          cmake -S . -B build
          cmake --build build --target check

